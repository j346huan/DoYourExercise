On $x\ne0$, normalize $x=1$; the equation gives $y=zw$, so this open subset of $Q$ is $\mathbb A^2$. Hence $Q$ is birational to $\mathbb P^2$. By \exref{I.2.15}(b), $Q$ contains disjoint closed curves. Any two closed curves in $\mathbb P^2$ meet by \exref{I.3.7}(a), so $Q$ is not isomorphic to $\mathbb P^2$.
