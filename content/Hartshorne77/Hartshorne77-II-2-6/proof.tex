The zero ring has no prime ideals, so its spectrum is empty. There is exactly one continuous map $\varnothing\to X$, and the accompanying maps $\mathcal O_X(U)\to\mathcal O_{\varnothing}(\varnothing)=0$ are unique. There are no stalk conditions to check. Thus $\operatorname{Spec}0$ is initial.
