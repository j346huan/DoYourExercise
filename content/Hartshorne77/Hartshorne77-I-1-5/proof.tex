If $B=A(X)$, then $B=k[x_1,\ldots,x_n]/I(X)$ is finitely generated and reduced, since $I(X)$ is radical. Conversely, write $B=k[x_1,\ldots,x_n]/I$. If $B$ is reduced, then $I$ is radical, so the Nullstellensatz gives $I=I(V(I))$. Thus $B=A(V(I))$.
