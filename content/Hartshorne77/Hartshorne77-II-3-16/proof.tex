If the assertion fails, noetherianity gives a minimal closed subset $Y$ for which $\mathcal P$ fails. Every proper closed subset of $Y$ satisfies $\mathcal P$, so the hypothesis implies that $Y$ does also, a contradiction.
