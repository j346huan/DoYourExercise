Choose $u_i\in M$ with $\varphi(u_i)=e_i$, and define $s:A^n\to M$ by $s(e_i)=u_i$. Then $\varphi s=1$, so $M=\ker\varphi\oplus s(A^n)$. The map $1-s\varphi$ projects $M$ onto $\ker\varphi$. Its values on any finite generating set of $M$ generate $\ker\varphi$.
