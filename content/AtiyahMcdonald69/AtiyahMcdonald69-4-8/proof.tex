The ideal $(x_1,\ldots,x_i)$ is maximal in $k[x_1,\ldots,x_i]$, so all its positive powers are primary. Extend through the remaining polynomial variables and apply \exref{4.7} repeatedly. Extension commutes with products, so the resulting ideals are precisely $\mathfrak{p}_i$ and its powers.
