If $A$ is Artinian, it has finitely many prime ideals, all maximal. Thus every point of its spectrum is closed and every subset is closed, so the spectrum is finite and discrete.

A finite discrete spectrum is discrete. Conversely, in a discrete spectrum every point is closed, so every prime is maximal. A zero-dimensional Noetherian ring is Artinian by Theorem 8.5.
