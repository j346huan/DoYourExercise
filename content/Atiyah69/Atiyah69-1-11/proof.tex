\textbf{(i)} Follows from $(x+1)^2=x+1$.

\textbf{(ii)} $A/\mathfrak p$ is an integral domain where every element is idempotent. Since $x^2=x$ implies $x=0$ or $1$, this is a field with two elements and $\mathfrak p$ is maximal.

\textbf{(iii)} Given two generators $a,b$, we can replace them by $a+b$ because $(a+b)(a-b)=a^2-b^2=a-b$, which gives us $a$ and $b$.
