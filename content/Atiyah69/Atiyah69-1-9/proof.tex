$\Rightarrow$ Follows from $\sqrt{\mathfrak a}=\bigcap_{\alpha\subseteq\mathfrak p}\mathfrak p$.

$\Leftarrow$ Suppose $\alpha=\bigcap\mathfrak p$ and $x^n\in\mathfrak a$, then $x\in\mathfrak p$ for each $\mathfrak p$ and so $x\in\mathfrak a$.
