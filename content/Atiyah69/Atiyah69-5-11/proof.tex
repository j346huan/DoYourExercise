If $\mathfrak{q}\subseteq B$ contracts to $\mathfrak{p}\subseteq A$, then $A_{\mathfrak{p}}\to B_{\mathfrak{q}}$ is flat and local. By \exref{3.18}, its spectrum map is surjective. Apply \exref{5.10}(ii).
